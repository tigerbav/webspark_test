import 'package:flutter/material.dart';

class CompleteStatusIndicator extends StatelessWidget {
  const CompleteStatusIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('');
  }
}
